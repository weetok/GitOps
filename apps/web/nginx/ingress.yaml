apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-nginx
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  tls:
    - hosts: ["web.172.30.1.115.nip.io"]
      secretName: wildcard-dev-tls
  rules:
    - host: web.172.30.1.115.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web-nginx
                port:
                  number: 80
